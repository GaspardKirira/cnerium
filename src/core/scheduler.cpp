#include <cnerium/core/scheduler.hpp>

namespace cnerium::core
{
  // Intentionally empty (scheduler is header-only for now).
  // This translation unit is kept as an "anchor" for future extensions.
} // namespace cnerium::core
